# Embedding_Pipeline

#Install Doctr.io
CPU only
pip install python-doctr[torch]

GPU (CUDA)
If you have a GPU, install PyTorch with CUDA first (example for CUDA 11.8):

pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118